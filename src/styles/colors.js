import { DynamicStyleSheet, DynamicValue, useDynamicStyleSheet } from 'react-native-dark-mode';

export default dynamicStyles = new DynamicStyleSheet({
  container: new DynamicValue('#FFF', '#12121B')
});




